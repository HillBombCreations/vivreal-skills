{
  "description": "Guards proxy routes to ensure they use the createProxyHandler() factory pattern (deterministic path check — never fires outside src/app/api/proxy/**/route.ts)",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/proxy-route-guard.cjs\""
          }
        ]
      }
    ]
  }
}
