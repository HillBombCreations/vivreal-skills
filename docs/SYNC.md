# Docs-sync log

One row per sync of the vivreal-skills docs against the source repos. The `/skills-sync` command reads the **latest date in this table** as the baseline for its drift audit. Add a row every time a sync lands.

| Date | Skills commit | Scope |
|---|---|---|
| 2026-08-15 | (this sync — evening) | **Release-train deploy model, live in 5 repos.** Verified fact sheet: `docs/sync-2026-08-15-release-trains-facts.md` (supplements this morning's testing/proxy-count sync — that row below is untouched). Headline: "merge to `main` = prod deploy" is now DEAD in Vivreal_Portal_Mobile, VR_Secure_API, VR_CMS_API, VR_Main_API, and VR_Client_API — prod deploys only from the fixed `stable` branch via a Friday-cut/Monday-promote release train (staggered: Secure 15:00 UTC, CMS 15:15, Main 15:30, Client 15:45, portal 16:00). Outreach, Client_Auth, and everything else are unchanged (still deploy on push to `main`) — left untouched everywhere they're referenced. Two platform gotchas now documented everywhere the train is: (1) Amplify autobuild fires only for never-built commits, so a portal rollback needs a manual `aws amplify start-job ... --job-type RELEASE`; (2) GitHub Actions fires no push run for a force-push that rewinds a branch, so a backend rollback needs a manual `gh workflow run lambda_api.yml --ref stable`. Files touched: `vivreal-workflow/skills/shared-standards/SKILL.md` (Deploy Trigger column for Secure/CMS/Main/Client + new "Release trains (2026-08-15)" section); the 5 expert agent files (`secure-api`, `cms-api`, `main-api`, `client-stack`, `portal` under `vivreal-experts/agents/`) + their 5 knowledge-skill counterparts (`vivreal-{secure-api,cms-api,main-api,client-stack,portal}-knowledge`) each gained a release-train gotcha with that repo's promote time + rollback rule + `docs/RELEASE.md` pointer (portal additionally: `release.json` endpoint, `stable` branch + CloudFront E39DUKXYGXCX8Q origin swap, `main` = canary); `vivreal-knowledge/skills/vivreal-renderer-knowledge/SKILL.md` (portal deploy-model line corrected); `vivreal-infra/skills/vivreal-lambda/SKILL.md` + `references/deploy.md` (generic Lambda CI/CD claim split into the 4 release-train backends vs the still-push-to-main ones). Repo-wide grep for surviving "push to main deploys"/"merge = deploy" claims found no other in-scope hits — deliberately left: `vivreal-experts/agents/outreach-api.md` + its knowledge counterpart (Outreach, still true), `vivreal-infra/skills/vivreal-site-deploy-pipeline/SKILL.md` (Templates' unrelated promote-stable model), `vivreal-knowledge/skills/vivreal-mcp-server-knowledge/SKILL.md` + `vivreal-analytics-knowledge/SKILL.md` (VR-MCP-Server / VR_Analytics_API, neither in the 5), and the `vivreal-workflow` command/skill "never push to main" lines (agent git-branch discipline, unrelated to backend deploy triggers). No new agents/skills created — runbooks live in each repo's own `docs/RELEASE.md`, per user decision to extend existing docs. Versions: marketplace 1.12.0→**1.12.1**; workflow 1.7.0→**1.7.1**, experts 1.3.0→**1.3.1**, knowledge 1.7.0→**1.7.1**; mirrors regenerated via `sync-to-agy.js` (5 expert mirrors touched) |
| 2026-06-24 | 4c5698a | Expert/knowledge/infra docs synced to code; promptify shipped |
| 2026-07-13 | (this sync) | Full sync: 8 source-repo CLAUDE.mds refreshed (Outreach CLAUDE.md created); portal/outreach/secure/cms/main/client/renderer/templates docs updated; 4 new knowledge skills (analytics, outreach-mcp, content, migrator); brand-voice merged with vivreal-content 01-voice-and-rules (canonical); prompt playbook +2 scenarios; proxy-factory manual allowlist 18→28; /skills-sync guardrail added |
| 2026-07-15 | (this sync) | Targeted branch-model sweep after site-provisioning Phase 2 shipped: per-site branches → shared `stable` channel + promote-stable release workflow. Updated: site-deploy-pipeline, deploy-tracker, deploy-nudge hook, eventhandler-knowledge, templates-knowledge, renderer-knowledge, vivreal-sites (+2 refs), experts event-handler (agent+skill). `sync_conflict`/`templateSyncWebhook`/auto-sync references marked dead |
| 2026-07-25 | (this sync) | Targeted commerce sweep after sites-commerce Phase 3 shipped: renderer-knowledge 1.35.1→**1.38.0** (+1.36.0 `isPageAllowed` page-gate export, 1.37.0 relative-palette resolution, 1.38.0 provider-agnostic `checkoutIdentifier`); templates-knowledge renderer dep ^1.35.1→^1.38.0 + new "Storefront checkout path" section (`PAYMENTS_PROVIDER_TYPES` local mirror, 4-hop identifier whitelist chain, strip-types testability extraction pattern, lockfile-float canary note). Known-stale left in place: renderer CLAUDE.md version header (1.35.1 — refresh with next release commit; docs-only master push would trigger publish.yml). vivreal-knowledge 1.5.2, marketplace 1.9.2 |
| 2026-07-30 | (this sync) | Full sync after ~640-commit drift across 16 repos (5 parallel audits). Counts re-verified: portal 180→**189** routes (157+32; classify by `_helpers/createProxyHandler` module path), Secure 12→**13** Lambdas (+instantiateTemplateWorkerDlqConsumer), Main 3→**4** (+NotificationConsumerFunction), EventHandler 27 = **12**+9+6 (arithmetic fix), Outreach MCP 58→**50** tools/9 modules/0 prompts (prospects retired), renderer 1.38.0→**1.42.1**, schemas →**1.29.0**, tier-quotas →**3.1.0** (aiSiteEditing/aiComponentGen), site-loader →**0.2.4**. Major new subsystems documented: CMS content go-live scheduler, AI agent stack, billing lifecycle Phase 3, worker durable runState + DLQ, sticky dbKey (Phase 5), Shopify webhook receiver, notification queue. Resolved-gotcha sweep: featureFlags route drift RESOLVED, overageBilling-undeclared RESOLVED (schemas 1.27), W12 API-402 neutralized, Route53 edge cutover DONE, Square kill switch retired, auditRetentionDays claim was FALSE. Honesty-floor sync: publish-to-email + live-preview-parity claims removed everywhere (incl. vivreal-cms `launch-content-everywhere`; fulfill-order/create-payout phantom tools deleted). NEW: `vivreal-agent-knowledge` + `vivreal-infra:vivreal-notifications` skills, `sites-stack` expert, 4 playbook trigger rows. FIXES: promptify "run it" now delegate-only (Skill/Agent tool with plugin-qualified names); proxy-guard +`media/share-image`, module-path check; proxy-route generator de-footgunned (prod-host fallbacks, per-upstream ctx params); design-system dark-mode rule → light-only; line-number citations stripped (10 files); **db-explorer repointed hb-api-secrets→vivreal/prod/main-api (secret was deleted in AWS 2026-07-20 — launcher was silently broken)**. Source-stale CLAUDE.mds flagged for repo-side refresh: CMS (07-21), Templates (07-21), EventHandler (07-21), renderer (1.39.2), Secure (07-26, pre-worker/flags work + still asserts resolved featureFlags drift), portal (fresh but claims hb-api-secrets for CTX_SECRET), vivreal-content (06-25). Versions: marketplace 1.10.0; experts 1.2.0, knowledge 1.6.0, infra 1.3.0, workflow 1.6.0, product 1.1.3, proxy-factory 1.1.0, cms 1.0.1, fullstack 1.0.3, db-explorer 1.1.0 |
| 2026-08-15 | (this sync) | Sync after the 2026-08-11→15 **fleet testing/lint/prod-bug campaign** (portal PR #274 + 5 backend PRs, all merged & deployed; Amplify job #456 SUCCEED) plus 9 days of general drift (~380 commits across 14 repos). Verified fact sheet for every claim: `docs/sync-2026-08-15-facts.md`. Counts re-verified: portal proxy routes 189→**198 (167 factory + 31 manual)**; Secure Lambdas 13→**15** (+ShopifyTokenRefreshFunction +AlarmVerifierFunction); renderer 1.42.1→**1.50.0**; MCP tool counts 69/50 unchanged; schemas 1.29.0 unchanged; Main 4 / Analytics 2 (LIVE) / EventHandler 27 unchanged. **Portal testing regime documented in shared-standards + portal expert/knowledge**: signed-ctx e2e auth (`e2e/fixtures/ctx.ts`, test server :3100 with `.next-test` distDir, mock upstream :4600 returning RAW backend shapes), coverage-map gate (`e2e/coverage-map.json` + `check-coverage-map.mjs --strict`), `e2e/BASELINE.md` as the authoritative inventory (619 tests/96 specs), Vitest unit layer (~257 files), repo ESLint **0/0** (flat config; `react-hooks/*` scoped off `e2e/**`; generated artifacts ignored — flat config reads no .gitignore), husky pre-commit/pre-push as the ONLY gates (**no CI** — deliberate). "Backend testing conventions" table rewritten: every backend now has ESLint + husky + coverage gates (Secure 100/100/100/100, CMS 100%, Client 100%-branch, Outreach 98%, Main ratchet + ⚠️ real-email warning, Client_Auth harness). **New proxy conventions**: manual routes unwrap upstream errors via the now-exported `extractUpstreamError()`/`extractUpstreamDetail()` from `_helpers` (bare-string Main-API error bodies killed the login error branches); `src/proxy.ts` short-circuits ALL `/api/proxy/` paths after rate limiting (proactive ctx refresh on a proxy request desyncs CSRF — the agent/execute bug class). proxy-route-guard allowlist +2 (`outreach/demo-link/[code]` public GET; `user/delete-account` 409-body preservation). **Prod-bug punch list** (`Vivreal_Portal_Mobile/docs/projects/portal-frontend-testing-strategy/prod-bugs-found.md`) pointered from all 5 backend knowledge files; per-backend fixes recorded: Secure cross-tenant IDOR pattern (`{_id, groupID}` service filters + validated-group controller precedence) + err.code forwarding (CancelFlow retention live), Client filter-drop fix + bounded fan-out (max 12 keys / 5 checks), Main seat-gate 0-seat + prototype-key bypasses closed, Outreach tenant-scoped sender maps + SNS replay guard, Client_Auth bundle trim + timeout hardening. Non-campaign drift synced: CMS dashboard-insights aggregate, renderer 1.47–1.50 kit waves (RESALE-1 "Marlowe & Kept", wrinsy), Templates Next 16.3.0, Migrator site-loader 0.3.9 + wrinsy cutover, EventHandler site-registration-at-creation + pool-leak fix, VR-MCP dbKey-routing/tier fixes, Secure dep pins corrected (renderer 1.47.0 exact, site-loader ^0.3.5). Source-stale CLAUDE.md flagged: vivreal-site-renderer (2026-07-27 vs heavy Aug drift). Versions: marketplace **1.12.0**; experts **1.3.0**, knowledge **1.7.0**, workflow **1.7.0**, proxy-factory **1.2.0**, fullstack **1.0.4**; mirrors regenerated via `sync-to-agy.js` (8 expert mirrors) |
| 2026-08-06 | (this sync) | **New plugin `vivreal-agent-review` (v1.0.0)** — read-only reviewer for Claude Code *definition* files (agent/skill/command/plugin `.md`+`.json`), not product code. Ships the `definition-reviewer` agent + `/definition-review` command + `references/` (corrected rule table, offline doc-verified platform-fact ledger, 6-case eval corpus from `vivreal-hq@29b9404`). Placement: standalone plugin, because `vivreal-workflow` fails `claude plugin validate --strict` at exit 1 today over 4 unrelated skill YAML-parse errors, so the "validates clean" gate is unreachable there. Registered in `marketplace.json` (13→14 plugins; `metadata.version` deliberately NOT bumped); `sync-to-agy.js` run with a scoped-diff guard, generating exactly 2 mirrors inside the new plugin and touching nothing else. Gates: `validate --strict` exit 0 for both the plugin and the marketplace root; the agent's own rule set run against its own plugin → 0 errors/0 warnings with both mirrors correctly excluded as byte-identical. **OQ-2 settled empirically:** `claude plugin validate` DOES cover agent files (`Validating agent:` lines) but on an agent checks only frontmatter parse + presence — not `name` charset, `model`, `color`, unknown fields, or tools — so every field-level agent rule is Stage-1-only in both profiles. Rule set corrected against the kit in 15 places, the load-bearing ones being `when_to_use` added to the 20-key command/skill set (the only kit drift that false-positives on valid input), the missing `ref-unresolved` rule with its bare-basename exclusion, `claim-false` promoted to first-class, the Agency `[policy]` bundle defaulted OFF, and new `*-frontmatter-parses` rules. Corrections found only by running it for real: `ref-unresolved` needed a monorepo suffix fallback, a fenced/indented-sample exclusion, and an extension-segment guard, which together took a live run from 55 errors to **4** on a live checkout / **5** on the `git archive` eval reconstruction (3 `*-frontmatter-parses` + 2 `ref-unresolved`: `short-form-editor.md:160`, a genuinely dead path, and `footage-recorder.md:22`, which cites a git-ignored file `git archive` drops — present only on the reconstructed tree; 2 output-path `ref-unresolved` findings remain NOTEs). Superseded figures: an earlier "6" counted all output paths as errors, and an intermediate "3" counted `short-form-editor.md:160` as an output-path NOTE on a verb the file does not contain. Two rule ambiguities were given explicit tiebreaks to make these deterministic: the worked-example corollary now beats the output-path verb test, and clause 6's "four-space indented block" is defined as a CommonMark indented code block with list continuation never a block; clause 3 also gained a closed-TLD bare-hostname reject (`instagram.com/p/`). **Reported, not fixed (out of scope):** the 34 skills in `vivreal-knowledge`/`vivreal-infra`/`vivreal-workflow` that load with empty metadata from an unquoted `: ` in frontmatter, plus 3 newly-found instances of the same defect in `vivreal-hq` at HEAD (`carousel-editor.md`, `contact-enricher.md`, `commands/carousel.md`) that no prior check could see. Kit file itself left unedited — both doc URLs re-verified 200 with zero drift, so the brief's conditional kit-fix did not trigger; 17 kit corrections recorded as advisory in `docs/projects/agent-definition-reviewer/design.md` |
| 2026-07-21 | (this sync) | Full sync after ~360-commit drift burst (tier-quotas v3.0.0 sentinel scheme + overage billing; client.vivreal.io CloudFront edge CDN; migrator `/template` identity-kit pipeline + 3 new agents; analytics LIVE; outreach prospects store). Counts re-verified: portal 165→180 routes (149+31), Secure 11→12 Lambdas, EventHandler →27, CMS MCP ~72→69 tools, Outreach MCP 50→58, renderer 1.29.3→1.35.1, schemas →1.25.0. All experts + knowledge skills re-stamped; shared-standards repo/Lambda/secrets tables rewritten (hb-api-secrets retired → vivreal/prod/*); proxy-guard manual allowlist 28→31; playbook + trigger row for `/template`. BUGFIX: `vivreal-writing-plans`/`vivreal-brainstorming`/`vivreal-subagent-driven` invocation references now plugin-qualified (`vivreal-workflow:` prefix) — bare names return "Unknown skill". Same-day follow-up: all 8 stale source CLAUDE.mds refreshed and pushed (Portal 84d20c2, Secure d20cd14, renderer d2c282b, CMS 0502a91, Client 7323e7b, Main 0deae94, EventHandler 4b110ed, Templates branch-model 5595b5e); skills-doc staleness caveats swept to "refreshed 2026-07-21"; corrections: flat `siteData.motionPreset`, EventHandler 27 = 11+1+9+6. Still no CLAUDE.md (intentional, README/docs are truth): Outreach API, Analytics, Schemas, Migrator. Known-thin: CMS CLAUDE.md Routes list |

## Conventions established 2026-07-13

- Every expert agent + knowledge skill that embeds source-repo facts carries a `Last synced: YYYY-MM-DD` line near the top of its body.
- Expert/knowledge docs cite **function/route/file names, not line numbers** — `src/foo.js:123` citations rot within weeks; `findEnrollmentByMessageRefs` doesn't.
- The canonical brand-voice source is `C:\repos\vivreal-content\knowledge\01-voice-and-rules.md`; the `vivreal-brand-voice` skill mirrors it.
