---
name: vivreal-renderer-knowledge
description: Use when working in vivreal-site-renderer (the @hillbombcreations/site-renderer npm package) — the rendering engine that turns Vivreal site config into a live website, consumed by both Vivreal_Templates (customer sites) and the portal Studio live-preview. Covers the publish-to-GitHub-Packages release runbook (publishing hits every live customer site), the ImageComponent injection seam, the Tailwind v4 @source requirement, the powered-by-Vivreal tier gate (canHidePoweredBy), the provider-agnostic checkoutIdentifier (Stripe/Square), the isPageAllowed page-gate export, motion-signature presets, the bakery identity kits and catalog/craft/profile page templates, prefers-reduced-motion, and the DESIGN_LANGUAGE.md authority. Triggers on: vivreal-site-renderer, site-renderer, @hillbombcreations/site-renderer, renderer package, ContentRenderer, PageTemplates, publish renderer, bump renderer, primitives, design language, canHidePoweredBy, checkoutIdentifier, DetailProductData, CartAdapter, isPageAllowed, page gate, DemoRibbon, motion preset. Source of truth: C:\repos\vivreal-site-renderer\CLAUDE.md (still dated 2026-07-27 — stale against heavy August drift; trust package.json/git log over it) + docs/DESIGN_LANGUAGE.md.
---

# vivreal-site-renderer — knowledge digest

Last synced: 2026-08-15 (published version **1.50.0**, up from 1.42.1 at last sync — CLAUDE.md is still dated 2026-07-27 and has not kept pace with the heavy August release drift; trust `package.json`/git log over it)

The **rendering engine** (`@hillbombcreations/site-renderer`) that turns Vivreal site config (pages + collections + pageConfigs) into a live website. Consumed by `Vivreal_Templates` (customer sites) and `Vivreal_Portal_Mobile` (Studio live-preview) — both pull from GitHub Packages at build time. React 19 peer, next `^15||^16` peer, TypeScript, framer-motion 12, Tailwind classes in source. `C:\repos\vivreal-site-renderer\CLAUDE.md` + `docs/DESIGN_LANGUAGE.md` give depth — CLAUDE.md last refreshed 2026-07-27 at 1.39.2 (header "Current published version: 1.39.2"); it now trails the entire 1.40–1.50 wave (musician kit, BrandMark, cutout/editorial commerce, section-photo/full-bleed layouts, wrinsy parity kits, RESALE-1) — trust `package.json`. For the site product/authoring model that drives this renderer (page formats, sections, Studio↔live composePage parity) see `vivreal-sites`.

## Structure

Key src dirs: `src/chrome/` (Navbar/Footer/MobileNav/OverlayNav/CardNav/UtilityStrip/SectionShell + `SocialIconRail.tsx`, `DemoRibbon.tsx`, `poweredBy.ts`, `deriveNav`/`resolveChrome`), `src/components/` (section building blocks: FloatingCta, ConfigurableForm, FooterView, `BrandMark.tsx`, `CapturePanel.tsx`, `ListenRow.tsx`, …), `src/HomeSections/`, `src/PageTemplates/` (products/shows/team/menu/schedule/collection + `CatalogStorefront`, `DiscographyIndex`, `ComposedPageSkeleton`), `src/DetailPage/`, `src/layouts/` (one file per layout dispatchId), `src/primitives/` (editorial: SectionMasthead, MonoMeta, EditorialCard, Spotlight, CategoryChipRail, RichText, SaleBadge; filtering: FilterSheet, FacetFilterBar, FilterPillStrip), `src/composition/` (`blocks.ts`, `defaultBlocks.ts`, `panoramaPanels.tsx`), `src/lib/` (`discography.ts`, `listenLinks.ts`, `mediaCarousel.ts`, `spotlightCarousel.ts`, `agendaList.ts`, …), `src/registry/` (`registry.ts` = the canonical dispatchId source), `src/tokens/` (`motion.ts`), `src/agent/` (`./agent` subpath export), `src/context/SiteRendererContext`, `ContentRenderer.tsx` (top-level dispatch), `index.ts` (public barrel). `dist/` is generated — **never hand-edit** (gitignored; CI builds it). Tests: `npm test` = SSR-render assertions via `renderToStaticMarkup` under `node --test`.

## The 2026 buildout (1.5.0 → 1.50.0) — what exists now

- **1.36.0 — the page-gate rule is package-owned** (`src/chrome/pageGating.ts`, W10.12): exports `isPageAllowed` + `normalizePageSlug` + the `PageGate` type for the `pages` gate on chrome configs (EmailPopupConfig, AnnouncementStripConfig). ENFORCEMENT stays consumer-side by design — the two consumers differ in both inputs (current page: Templates derives from `usePathname()`, Studio preview is handed the active slug; unauthored-gate meaning: caller supplies `fallback` — every-page for the strip, home-only for the popup). Templates retired its inline mirror when bumping to 1.36.0; never re-implement the rule in a consumer.
- **1.37.0 — relative per-page palette values resolve to concrete colors**: `resolvePagePalette` now DEREFERENCES `var()`/`color-mix()` palette-preset values before the renderer's `resolveColor` sees them (which rejects non-concrete colors — the Studio F9 "page color treatment is inert" bug).
- **1.38.0 — provider-agnostic checkout identifier** (sites-commerce Phase 3): `DetailProductData.checkoutIdentifier?: Variantable<string>` — the value the cart places in the checkout POST's `products[].price` wire slot, shared verbatim across providers (Stripe price id = `default_price`; Square catalog `variationId`, scalar-only — Square is single-variation by design). `contentItemToProduct` populates it as `raw.checkoutIdentifier ?? raw.default_price ?? scalar raw.variationId`; the actual payments provider resolves SERVER-side in VR_Client_API, so a wrong/missing identifier is an empty-catalog/failed-lookup outcome, never a mis-charge. Consumers resolve `checkoutIdentifier ?? default_price` — Templates ≥ PR #77 threads it through its cart projections. Note the renderer's own products data-source fallback (`composition/blocks.ts`) STILL statically assumes `'stripe'` for an unstamped `integrationProvider` binding — since 1.39.4 that fallback path emits a WARN, and universal binding stamping (site-loader 0.2.0 seeding + Studio block-linker) is the mitigation.
- **1.39.0 — studio batch**: `src/chrome/Navbar.tsx` gains controlled menu state (`menuOpen`/`onMenuOpenChange`; the hamburger is a TOGGLE; state resets on `menuStyle` change) + a nav auto-collapse safety net; nav reveals use composite path+index keys (fixes duplicate-key crashes); AnnouncementStrip gains a `data-announcement-strip` hook; hero trust indicators become text-only (the icon field is inert legacy data); CTASection email capture wins over background media.
- **1.39.1 → 1.39.2 — controlled-open menu in Studio preview** (1.39.2 REVISES 1.39.1): 1.39.1 made the controlled-open menu render at ALL breakpoints in Studio preview and added a legibility scrim on the fullscreen menu background (OverlayNav, `SPOTLIGHT_SCRIM_MIN_ALPHA`). 1.39.2 revised the desktop behavior: desktop controlled-open preview now opens the first children-bearing NavDropdown flyout via a module-private `forceOpen` prop — NOT the mobile drawer (1.39.1's drawer force was reverted).
- **1.39.3**: MobileNav side effects (scroll lock) gated on visibility; composite BannerLayout badge keys.
- **1.39.4**: provider-neutral cart/checkout copy + the WARN on the unstamped-provider stripe fallback (above).
- **1.40.0 — musician identity kit "poster-pop"**: 10 fleet-safe surfaces across `chrome/Navbar`, `Footer`, new `chrome/SocialIconRail.tsx`, `components/ListenRow.tsx`, `components/PageHero.tsx`, `HomeSections/CTASection.tsx`, `composition/panoramaPanels.tsx`, `PageTemplates/DiscographyIndex.tsx`, `layouts/MediaCarouselLayout.tsx`, `layouts/SpotlightCarouselLayout.tsx`, `lib/{discography,listenLinks,mediaCarousel,spotlightCarousel}.ts`.
- **1.40.1**: `lib/agendaList.ts` / AgendaListLayout — exact UTC-midnight timestamps treated as date-only in the itinerary re-anchor (hydration fix).
- **1.41.0 — musician dedicated-page components**: `components/CapturePanel.tsx` (dispatchId `capture-panel`, kind static, `nullOnEmpty:false`), `layouts/FilmStripLayout.tsx` (`film-strip`), `layouts/PostcardStripLayout.tsx` (`postcard-strip`); `CapturePanel` + `CapturePanelProps` exported from `src/index.ts`.
- **1.42.0 — cutout storefront + editorial detail + BrandMark**: ProductCard (`PageTemplates/Products/parts.tsx`) gains `tileStyle: 'cutout'` (heritage-editorial commerce card: square, white media plate, quiet hover); SortDropdown gains `square`, FilterSidebar gains `editorial`; DetailPage gains `styleVariant: 'editorial'` (`types/DetailPage.ts`) driving flat token-surfaced panels / square polaroid media / display-font title in DetailHero, DetailVariants, DetailAddToCart. New primitive `components/BrandMark.tsx` (+ `brandInitials`) exported from `index.ts`.
- **1.42.1**: `BrandMarkProps.fill` — the avatar box fills its parent (100%/100%) instead of a fixed pixel size, so callers own responsive sizing via classes; `size` still scales the letter-mark.
- **1.47.0 — section-photo / hero.meta / full-bleed layouts**: a new wave of layout surfaces (photo-forward section format, hero metadata treatment, full-bleed layout variants) — check `package.json`/git log for exact dispatchIds, CLAUDE.md predates this wave.
- **1.48.0–1.49.1 — wrinsy migration parity + home-tail kits + CTA band variants**: kit and layout additions produced to support the wrinsy site migration cutover (see `vivreal-migrator-knowledge`), including new home-tail composition kits and additional CTA band variants.
- **1.50.0 — RESALE-1 "Marlowe & Kept" identity kit + coordinated-products fix**: a new resale/thrift-vertical identity kit, plus a fix to coordinated-products handling (the grouped-variant products binding).

- **The registry is the source of truth** — grep `src/registry/registry.ts`, not memory. Layout dispatchIds now run ~50 deep (pricing/faq/steps/comparison/tabs/link-cards/video/channel-diagram/… plus the July wave: `jump-links`, `calendar`, `agenda`, `locations`, `logo-wall`, `app-band`, `product-tiles`, `quote-carousel`, `story-panel`, `collage-strip`, `slideshow`, `category-directory`, `craft-split`, `index-list`, `social-panel`, `spotlight-panel`, `feature-split`, `longform`, `manifesto`, `positions`, `showcase-gallery`, `editorial-sections`, plus the musician wave: `media-carousel`, `spotlight-carousel`, `film-strip`, `postcard-strip` — all kind `layout`). Static blocks include `section-header`, `about`, `bio-panel`, `reservation`, `capture-panel` (`defaultBlocks.ts` appends it when no capture-panel block is present).
- **Page-template dispatchIds grew**: `catalog` (CatalogStorefront — category-rail sectioned browser for large categorized catalogs), `craft` (savoir-faire/heritage story) and `profile` (meet-the-maker) — both delegate to standard composed sections; `location-hub` composes purely via `defaultBlocks.ts` (the format's value is semantics; no bespoke template); `panorama` (color-block anchored panels via `composition/panoramaPanels.tsx`) and `discography` (`DiscographyIndex.tsx`).
- **Four identity kits** — three bakery (1.30→1.35) + musician **"poster-pop"** (1.40.0–1.41.0, the surfaces listed in the version wave above). The bakery three: **Levain** (split-product hero, mega dropdowns, StoryPanelLayout, QuoteCarouselLayout quote band, centerpiece footer, menu `itemStyle:'tiles'` cards via MenuTileCard + a PDP for menu items), **Poilâne** (CategoryDirectoryLayout / CraftSplitLayout / IndexListLayout landing comps, `tileStyle:'cutout'` menu cards, in-grid promo tiles, stamp badges), **Ansel** (ghost mastheads, gallery catalog, LongformLayout + the `profile` page format, boutique contact form). Shared kit primitives: UtilityStrip, FilterPillStrip, ManifestoLayout, PositionsBoardLayout, StepsLayout.
- **Motion-signature presets** — `src/tokens/motion.ts`: `MotionPreset`, `MOTION_PRESETS`, `resolveMotionPreset`, `motionPresetVars`. Named site-wide motion identities selected via the FLAT `siteData.motionPreset` field (not nested under `theme`), stamped on the DOM as `data-motion-preset`. Current `MOTION_PRESETS` has 9 ids: `default`, `stately`, `taproom-kinetic`, `warm-drift`, `atelier`, `clinical-calm`, `punchy`, `confection`, `encore` (the musician-kit identity).
- **DemoRibbon** (`src/chrome/DemoRibbon.tsx` + `resolveDemoRibbon.ts`) — floating pill for demo sites: sibling-theme switcher, hub-URL row, swatch dots, claim CTA; deliberately styled off the Studio palette, not the site palette. **ShowcaseGalleryLayout** = 3-up theme-comparison gallery (kind `layout`); **EditorialSectionsLayout** = magazine-band info pages.
- **Faceted filtering** — FilterSheet + FacetFilterBar driven by `sectionConfig.facets` (server-side narrowing via `sectionConfig.scope`), gated by `filterUi`: when absent, the legacy chips path renders byte-identical.
- **Config surface** (earlier wave, still current): `SiteData.floatingCta`, `SiteData.chrome: 'dark'|'light'`, per-page `PageConfig.palette` (rebase via `resolvePagePalette` with a WCAG guard), per-section `background:'dark'` band (SectionShell re-scopes palette vars), `navigation.headerWidth: 'contained'|'full'` + `megaMenu`, footer `socialStyle: 'column'|'icons'` + `newsletterPlacement: 'brand'|'bar'`, `sectionConfig.featureImage`/`detailEligible`, `PageHero.background {gradient|image|video}` + `emailCapture` + `variant`, `labels.animatedHero`, `labels.heroMotif`, `CTASection config.gradient`. Fleet-safe migration knobs (default-absent — no visual change until set): `navigation.navLinkColor`, `hero.eyebrowStyle`, `dropdownStyle:'cards'`.
- **Skeletons**: `ComposedPageSkeleton` (structure-derived; `products` + `collection-list` arms render real `CollectionPage` in loading mode).
- **`ResponsiveImage` is the srcset chokepoint**: raw `<img srcSet>` for CMS derivatives (next/image ignores manual srcSet), delegates to the injected `ImageComponent` otherwise. Gallery `sizes` now tracks column density; logo srcset tracks the rendered box. Route new image surfaces through it.

## Publishing = a production gate (it hits every live site)

The renderer is a published npm package; a publish reaches every deployed customer site on their next rebuild. Release runbook:
1. Edit `src/`, `npm run build` (tsc → dist), commit **src + package.json only** (dist is gitignored).
2. **Bump the version** (semver — patch/minor; major unused). Never republish a version (GitHub Packages rejects it).
3. **Push to `master`** → `.github/workflows/publish.yml` runs `npm ci` + build + `npm publish` automatically. (Local `npm publish` is discouraged + 403s with the read-only hb-api-secrets token; CI uses its own write token.)
4. **Bump consumers** — `Vivreal_Portal_Mobile` + `Vivreal_Templates` `package.json` → new version → clean lockfile regeneration → commit each separately.
5. Deploy: portal now ships via its own release train, NOT a merge to `main` (2026-08-15 — `main` is an Amplify build canary; prod promotes from `stable` on a Monday 16:00 UTC cron, see `vivreal-portal-knowledge`); Templates releases via the **promote-stable** workflow (main→stable FF) — merging Templates `main` alone releases nothing; the stable push rebuilds every site app. Expect a rollout wave after promote-stable.

Rollback: ship a new patch (don't `npm unpublish`); consumers can pin an older version as an emergency lock. **Dependency discipline**: this package must stay installable with a single GitHub Packages token — adding a second private `@hillbombcreations/*` dependency 401s customer Amplify builds (this killed 1.35.0; see the powered-by gate below).

## Key patterns

- **`SiteRendererContext`** — the injection seam: `ImageComponent`/`LinkComponent` (typed `ElementType`), `previewMode`, `CartAdapter`, `mapsApiKey`, `onSubscribe`. The renderer does NOT import `next/image` (that would force Next.js on consumers). Default is plain `<img>`; Next consumers inject `next/image` via a provider. Studio preview strips it to plain `<img>` deliberately.
- **Powered-by-Vivreal tier gate** — `src/chrome/poweredBy.ts` owns `canHidePoweredBy(tier)` against a **local** `Set(['basic','pro','proplus','enterprise'])` (note: `basic` CAN hide the badge). Deliberately NOT an `@hillbombcreations/tier-quotas` import — that dep 401'd the 1.35.0 publish (customer Amplify build tokens can't read a second private GitHub Packages package), so 1.35.1 reverted to the local set and re-exports `canHidePoweredBy` from `src/index.ts` (Templates imports it from the renderer). **Keep the local set in lock-step with tier-quotas `TIER_FLAGS.*.canHidePoweredBy`.** Consumed by the renderer `Footer` and the Templates `StaticPageTemplate`.
- **BrandMark: a brand logo is NEVER center-cropped** (`components/BrandMark.tsx`, 1.42.0+). `shape='inline'` = height-constrained `object-contain` at natural width; `shape='avatar'` = fixed square `object-contain`, and when the loaded image measures wide (`wordmarkAspect` default 1.6) the `<img>` is replaced by a letter-mark avatar (`brandInitials`); `fallbackSrc` (typically `siteData.favicon`) is preferred over the letter-mark. Aspect detection is client-side at image load — contain-fit until then, so the SSR first paint never crops. `fill` (1.42.1) lets the avatar box fill its parent so callers own responsive sizing.
- **Respect `prefers-reduced-motion`** — all motion primitives fall back to static (`useReducedMotion()` / `motion-reduce:`).
- **Tailwind v4 `@source` requirement** — renderer utility classes silently no-op in v4 consumers unless the consumer's CSS `@source`-scans the renderer package/dist. Root cause of "padding missing / past shows not displaying" bugs. Portal `globals.css` `@source`-scans the renderer dist for Studio-preview parity.
- **ESM discipline** — every relative import carries an explicit `.js` extension; match that in new code.
- **Source is authoritative** — grep `src/`, not `dist/`. **Styling anything? Read `docs/DESIGN_LANGUAGE.md` FIRST** (primitives, palette-proofing via `color-mix`/CSS vars, the inline-layout "Tailwind-generation-proof" rule, `object-contain` for showpiece imagery).

## Gotchas + forward/backward skew

- **Config-less blocks are legit** — `blockToComposedSection` in `src/composition/blocks.ts` guards `block.config?.`: a Mongo/Mixed round-trip drops empty `config` objects, and before the guard `block.config.bindings` threw and killed the entire Server-Components render. Never assume `block.config` exists.
- `DetailSection` is a closed union: adding an enum value is a MINOR bump; changing existing semantics is MAJOR. Unknown section values from a future portal version render `null` (no crash).
- Versioning in older CLAUDE.md sections is historical — the published line moves fast; check `package.json` (currently 1.50.0; the CLAUDE.md header lags at 1.39.2, refreshed 2026-07-27 — refresh it only with the next release commit, never a docs-only master push, since ANY master push triggers `publish.yml`).
