---
name: vivreal-migrator-knowledge
description: Use when migrating an external website into the Vivreal CMS, turning an exemplar site into a reusable template, restyling an already-live site in place, or working in Vivreal_Site_Migrator. Covers all THREE modes. /migrate <url> — 1:1 DATA/STRUCTURE parity (crawl → agents → assemble → bundle → audit → Studio → user-gated cutover, 3 human gates, block/best-fit gap policies, the verify-live post-deploy gate). /template <exemplar-url> — 1:1 DESIGN/LAYOUT parity with placeholder content + a fictional brand, producing renderer identity kits that feed the portal template picker, now with a STUDIO GATE (confirm-studio + studio-confirm/studio-registrar agents). Live-site restyles — pull-live-site/apply-restyle/restyle-live-site scripts. Also: the PARITY STANDARD, two mandatory audit passes, static-crawl blind spots + platform-aware deep extraction (WooCommerce/Shopify/Squarespace APIs), the slug-depth ≤2 rule, the @hillbombcreations/site-loader package (0.2.x, runs in prod), and the CAPABILITIES.md manifest. Triggers on: site migration, Site_Migrator, /migrate, /template, restyle, identity kit, template picker, cutover, crawl, site audit, site-loader, studio gate, CAPABILITIES.md. Source of truth: docs/migration-flow.md + docs/template-flow.md (README.md updated but thin/migrate-only; NO committed CLAUDE.md).
---

# Vivreal_Site_Migrator — knowledge digest

Last synced: 2026-08-15

Agent-driven repo with **three modes**: `/migrate` (migrate an external site's content into the Vivreal CMS, 1:1 data/structure parity), `/template` (turn an exemplar site into a reusable renderer identity kit, 1:1 design/layout parity with placeholder content + a fictional brand), and **live-site restyles** (restyle an ALREADY-LIVE site in place — see the restyle section). The repo is the **orchestrator only** — renderer components live in the renderer repo; net-new components are authored there via the template track's `component-builder` agent, never as migrator code. There is **no committed CLAUDE.md** — `docs/migration-flow.md` + `docs/template-flow.md` are truth (`README.md` WAS updated — it carries the seo-redirects banner and links `docs/migration-flow.md` — but is still thin and migrate-only). Operational detail lives in TEN `.claude/agents/`: the five stage agents `{ingest,collections,integrations,site,cutover}.md`, the Playwright-enabled template-era trio `{kit-designer,component-builder,page-confirm}.md`, plus `studio-confirm.md` (the editability authority) and `studio-registrar.md` (portal-side palette/editor/whitelist registration). The loader is extracted into `packages/site-loader` — installable as `@hillbombcreations/site-loader` (now **0.3.9** (was 0.2.4 at last sync, via 0.3.5), running inside VR_Secure_API's `instantiateTemplateWorker` in prod; this skill owns it — see the site-loader section), exposing `instantiateBlueprint` so the portal TEMPLATE PICKER can instantiate published blueprints.

## /migrate pipeline + gates

`crawl` (`commands/crawl.js <url> --max-pages=120 --deep` — deep = a second Playwright engine in `src/capture/deepCapture.js`; probe-discovered dead-404 routes dropped by `src/capture/deadPageFilter.js`; Elementor body recovery via `collectTopLevelBlocks` in `src/capture/extractInPage.js`; Shopify catalog facets auto-enriched into `capture.commerce`, backfillable via `commands/enrich-commerce.js`) → `ingest` agent → `collections` + `integrations` agents (parallel; then the deterministic `commands/build-derived-collections.js` step, e.g. `buildHomeComparisonRows`) → `site` agent (LAST — binds only to keys collections emitted) → `commands/assemble-blueprint.js --gap-policy` → `commands/preview-bundle.js` + `serve-preview.js` → **audit** → Studio validation → `commands/load.js` (**user-gated cutover**). `captures/<domain>/` is gitignored and fully regenerable.

`/migrate <url>` runs it end to end with **3 human approval gates**: **Gate 1** pre-flight (URL + crawl scope, brand name — drives the `<key>.vivreal.io` subdomain, rendered brand, AND the Templates branch name — target group, gap policy, AND kit selection: `commands/select-kit.js` matches the vertical to an authored kit and writes `kit.json` — styling only, client brand always wins). **Gate 2** pre-creation (blueprint summary + per-page parity diff + screenshots; zero data/structure defects expected). **Gate 3** cutover (dispatches the `cutover` agent against production).

**Cutover:** default target is a **provisioned prospect-owned demo account** (VR_Main_API `provisionDemoAccount.js`, same brand name as assemble) — deploys noindex; pass `--live` ONLY at genuine go-live. `load.js --deploy` triggers the Deploy-Site Step Function via `POST /api/deploySite` (blank-template sites don't auto-deploy) and the `/api/revalidate` cache webhook; resume-safe via `load-state.json`. The loader drops pages bound only to zero-object collections (`convertBlocks` in the site-loader package). **Closing gate (MANDATORY after every deploy): `commands/verify-live.js`** — an HTTP-status sweep is NOT enough (Next streams a 200 shell and can `notFound()` mid-stream); it browser-drives every blueprint page checking hydrated-404 swap, a hydrated-content floor, TRUE-pixel image sharpness (density-corrected, re-decoded per `currentSrc`), and same-origin favicon links. **Hardened further post-wrinsy** — the wrinsy migration cutover (below) surfaced gaps this gate now closes; treat `verify-live.js` as the authoritative post-deploy check, not a formality.

**Wrinsy migration cutover** — a real `/migrate` run that exercised the pipeline end to end and fed fixes back in: closed parity-audit gaps (4A/4B live-DOM parity), Studio-editability-gate fixes (stage 11b), and capture/crawl fixes upstream in `src/capture/`. Treat it as evidence the audit gates catch real regressions, not a checklist step to skim.

## /template pipeline (identity kits)

`/template <exemplar-url>`: **scout → design gate → build (Round A/B) → validate**, with its own user gates. **GATE 1** — the `kit-designer` agent live-scouts the exemplar, writes the design brief (HANDOFF doc) + question set. **Round A** — kit authoring (persona + theme + chrome + home); **Round B** — the page walk, each exemplar page authored 1:1. **Validate** — `commands/{confirm-site,confirm-page,exemplar-diff,rebrand-capture,harden-demo,distill-kit}.js`; the `page-confirm` agent rates every section MATCH / ADAPTATION / SURPLUS / DEFECT. **GATE 3** ships only when the **kit quota** is met: Nav 1 · Header-hero variant 1 · Landing structured components ≥3 · Motion preset 1 · Page type (new `format`) ≥1 (knobs don't count). Kits live in `kits/authored/*.json`; three bakery kits shipped there (Levain, Poilâne/"Old Mill Bakehouse", Ansel). Musician template #1 **"poster-pop"** (MOTEL SATURN) shipped as a FOURTH industry template — but it was NOT distilled into `kits/authored/` (evidence it exists: renderer 1.40/1.41, site-loader 0.2.3/0.2.4, `docs/projects/industry-example-site-templates/HANDOFF-MUSICIAN-1.md` + `HANDOFF-MUSICIAN-PAGES.md`, `publish-out/musician-woozy`) — note the asymmetry so nobody hunts for a musician JSON kit. **RESALE-1 "Marlowe & Kept"** (resale/thrift vertical) shipped alongside renderer 1.50.0 and the site-loader 0.3.x line — the fifth industry template; ride the same release-train discipline (renderer publish → site-loader pin bump → capability manifest regen) before treating it as fleet-safe. `component-builder` is the aesthetic-matching component authority: TEMPLATE mode builds net-new in the renderer repo, MIGRATION mode reuses best-fit only. Publishing: `commands/publish-template.js` + `infra/site-templates-bucket.yaml` (S3) — the portal picker then instantiates via `instantiateBlueprint`.

**Studio-editability gate (template-flow stages 5b + 11b).** Stage **5b — Studio registration**: the `studio-registrar` agent handles portal-side palette/editor/whitelist registration for each kit surface. Stage **11b — STUDIO GATE**: `commands/confirm-studio.js` (page coverage · editor-stub scan · no-edit Save-All roundtrip) → STUDIO PASS, then the `studio-confirm` agent runs the agent edit walk with a classification ledger — verifying every kit surface is editable in the real portal Studio. Supporting modules `src/studio/{mintCookies,saveRoundtrip,siteValuesMerge,surfaceInventory}.js`; tests `tests/studio-save-roundtrip.test.js` + `tests/studio-sitevalues-save.test.js`; `commands/mint-studio-cookies.js` is now a thin wrapper over `src/studio/mintCookies.js`. Walk-2 benign save materializations are pinned as non-FAIL in `saveRoundtrip`.

**Vivreal Content template-site rule** (docs/template-flow.md): every site instantiated from a template (validation, demo, smoke, showcase) is created on the **Vivreal Content** group — groupName "Vivreal Content", key `vivrealcontent`, `_id 6a68169fe1457c2f3fd04530`.

**Release train** (template-flow.md §6b + `docs/fleet-release-train.md`): any kit that shipped renderer/site-loader surface rides renderer publish → site-loader pin bump (`^0.2.x` never auto-adopts) → capability manifest regen → manual `npm publish` of site-loader.

## packages/site-loader (0.2.x → 0.3.x) — owned here, runs in prod

`@hillbombcreations/site-loader` is now **0.3.9** (was 0.2.4 at last sync, via 0.3.5) and runs inside VR_Secure_API's `instantiateTemplateWorker` in production; this skill owns it. Release line:

- **0.2.0** — seeds pending integrations + stamps the products binding (`integrationProvider`) — the mitigation the renderer doc points at for the unstamped-provider stripe fallback.
- **0.2.1** — tag-scoped collection dedup in `packages/site-loader/src/loader/seedCollections.js` — stops cross-site absorption on shared tenants (a new site adopting another site's collections on the same tenant DB); pinned by `test/seedCollectionsDedupe.test.js`. Highest-severity loader fix; multi-tenant correctness.
- **0.2.2** — object-level checkpoints (resume granularity collection → object), hoisted dedup GET, width-4 media pool.
- **0.2.3** — musician template #1 pipeline (schema + capabilities + tooling).
- **0.2.4** — `labels.capturePanel` added to the strict PageLabels blueprint schema (`packages/site-loader/src/blueprint/schema.js`) — the bioPanel-precedent labels vehicle for the renderer's static `capture-panel` block (heading/subheading/body/buttonLabel/panelColor/image/imagePosition/anchor); the chrome-media walk re-stages the nested image slot generically. Plus `film-strip` + `postcard-strip` in `src/capability/composition.js` — NOTE the hand-lists: new layouts need a manual entry, not just a regen.
- **0.3.5 → 0.3.9** — schema and capability-manifest updates riding the renderer's 1.47–1.50 wave (RESALE-1 kit support, coordinated-products handling) — check `package.json`/git log for the per-patch breakdown; CLAUDE.md-equivalent docs here predate this line.

**Single integration-capability source**: `src/capability/readIntegrations.js` and `capabilities/integrations.capability.json` are DELETED, replaced by a site-loader-owned snapshot — `packages/site-loader/scripts/sync-integration-capabilities.js` parses the portal's MANIFESTS map (comment-stripped, so disabled entries drop out) → `packages/site-loader/src/capability/integrations.{json,js}`; `isConnectableProvider` is the gate the agents use; `--check` exits non-zero on drift. Two real bugs closed: the old readdir glob emitted facebook TWICE (meta.json + facebook.json both declare id "facebook") and offered shopify, which is commented out of the portal MANIFESTS (cannot be connected) — the migrator was scaffolding dead integration records. Re-synced 07-28 for LinkedIn CMA scopes + analytics.

## Live-site restyle tooling (the third mode)

Restyling an ALREADY-LIVE site in place — neither `/migrate` nor `/template`: `scripts/pull-live-site.js`, `apply-restyle.js`, `restyle-live-site.js`, `restyles/_shared.js` + per-site `dougskitchen.js` / `wavesofgrain.js` (+ `.media.json`), `upload-doug-media.js`, `upload-wog-media.js`, `spruce-doug-content.js`. Runbook: `docs/projects/oldmill-restyle-dougs-wog/HANDOFF.md` + `FINDINGS.md`; also `docs/projects/industry-example-site-templates/live-ops-motel-saturn/{restyle-motel-saturn,revalidate-motel-saturn,add-upcoming-dates}.js`.

**Media promotion preflight/repair**: `scripts/repair-site-media.js` + a preflight in `apply-restyle.js`, pinned by `tests/restyle-media-preflight.test.js` — closes two silent preupload-promotion failures (VR_Secure_API side, marked fixed in the FINDINGS doc). `.claude/agents/cutover.md` + `studio-confirm.md` carry the new checks.

## PARITY STANDARD (the pass/fail bar)

**/migrate: 1:1 parity with the live site on DATA and STRUCTURE** — every page, section, item, field, media asset, link, and nav entry present, nothing invented. **Gaps are DEFECTS to resolve, not residuals to accept.** Styling is the ONE allowed divergence (Vivreal's design system may look *better*). **/template inverts it:** the same 1:1 discipline applied to DESIGN/LAYOUT — content is placeholder and the brand fictional by design. Never declare a page done from the crawl alone.

## Audit passes + the post-deploy gate

- **4A — live-DOM parity sweep (MANDATORY, first):** for EVERY page, Playwright-load the LIVE site, scroll top-to-bottom, extract a fingerprint (headings, `<video>` + CSS `background-image` srcs, iframes, image/form counts, nav, every external href), diff against the blueprint. Any delta is a defect — recover from the live DOM and re-dispatch the owning agent.
- **4B — local render vs live:** serve-preview shim on `127.0.0.1:8799` + Templates dev server (`dev:linked` for unpublished renderer work), screenshots at 1280 + 375 for home, a nested page, and each content-type page, compared against live.
- **C — post-deploy live gate:** `verify-live.js` (above), run by the cutover agent after every load/cutover deploy.

## Gap policy (block / best-fit / partial)

- **`block`** — the Gate 1 default: assembly HALTS (exit 2) on any unresolved gap. The parity-run setting.
- **`best-fit`** — writes the blueprint with gaps recorded but exits 0 (partial-land); allowed ONLY with an explicit, per-gap user waiver. Never report best-fit coverage as faithful render. At assemble, `partial` is a friendly ALIAS for best-fit.
- `load.js` takes its own `--gap-policy block|partial` for seeding what's resolvable.

## Key heuristics (hard-won, verified live)

- **Static-crawl blind spots:** the crawl captures initial HTML, not the runtime DOM — it misses CSS-`background-image` media (hero videos, bio photos), lazy galleries, and JS-wired `href`s (press/gift-card/reserve/nav links). `--deep` recovers most; the 4A sweep is the backstop.
- **Platform-aware deep extraction:** detect the source platform and prefer its structured API over card scraping — WooCommerce `/wp-json/wc/store/v1/products`, Shopify `/products.json` (+ `capture.commerce` facets: variant sizes, option groups), Squarespace `?format=json`. Model `description` + `options` + `gallery` from day one; HTML-entity-decode names before matching.
- **Slug depth ≤ 2:** Templates routes only `[slug]` and `[slug]/[itemId]` — a 3+ segment source path preserved as a slug hard-404s even though it seeds fine. Flatten to a routable leaf and rewrite every nav/footer href.
- **⚠ Deep-slug 301s are applied by NOTHING** (`docs/seo-redirects-at-cutover.md` + the README banner): the blueprint's 301 map for slugs flattened past `MAX_SLUG_DEPTH` is computed but no component applies it — old URLs 404 once a site goes live. BLOCKING for the first cutover of any site with a non-empty redirects array.
- **Preview media descriptors carry a `name`** (`commands/serve-preview.js`) — unblocks required-image object saves in the Studio harness (defect D-1).
- **`verify-live.js` browser-boundary rule:** it destructures `PROSE_BLOB_CHARS`/`SHARE` into the `page.evaluate` callback — closure vars don't cross the browser boundary; keep constants passed explicitly.
- **WordPress `-WxH` thumbnail normalization:** `src/capture/imageUrl.js` strips trailing `-WxH` from `/wp-content/uploads/` image URLs (sized thumbnails 404; originals always exist).
- **Probe-guessed routes lie on WordPress:** guessed paths (`/menu`, `/about`, …) resolve to the theme's 404 template with a 200 — `deadPageFilter.js` drops them (probe-discovered pages ONLY; linked/sitemapped pages are never touched).

## capabilities/CAPABILITIES.md — useful to ANY Vivreal agent

Generated (`npm run gen-capabilities`) machine-readable manifest of every renderer format, layout, and component (dispatchId, kind, required backing fields, tags) — the authoritative answer to "can the renderer do X?". `capabilities/` now holds only `CAPABILITIES.md` + `collections.capability.json` + `renderer.capability.json` (integration providers moved to the site-loader snapshot, above). It reads the sibling renderer's **built dist**: a stale manifest surfaces FALSE gaps, so rebuild the renderer and regenerate before a run. Currently stamped renderer **1.41.0 / 85 components** — it does NOT yet know 1.42.x (`tileStyle: 'cutout'`, `styleVariant: 'editorial'`, BrandMark), so per its own rule it needs a rebuild+regen. The formats list includes `panorama` + `discography`; layouts include `media-carousel`, `spotlight-carousel`, `film-strip`, `postcard-strip`.

Also worth knowing: `docs/first-pass-fidelity.md` (live-fix ledger + migrator TODOs from real cutovers), `docs/standards/bakery-site-standard.md` (per-vertical quality bar layered ON TOP of parity), `docs/standards/leads-migrator-outreach-integration.md` (the Leads → Migrator → Outreach demo-site tie-in), plus the newer `docs/component-propagation-process.md`, `docs/fleet-release-train.md`, `docs/templates-composition-and-local-harness.md`, and `docs/seo-redirects-at-cutover.md`. Newer commands: `harness.js`, `studio.js`, `studio-env.js`, `qa-{art-direction,gate,live-parity,live-sweep}.js`, `regen-worker-manifest.js`, `validate-inventory.js`, `delete-site-content.js`. Env you'll hit: `VR_REPOS_ROOT`/`VR_RENDERER_REPO`/`VR_PORTAL_REPO`/`VR_SCHEMAS_REPO` (sibling-repo resolution), `CTX_SECRET`, `PORTAL_PREVIEW_URL`, `PORTAL_TOKEN_COOKIE`, `STUDIO_ADMIN_EMAIL`; runtime/cutover: `VR_GROUP_ID`, `VR_DB_KEY`, `VR_COGNITO_ID_TOKEN`, `VIVREAL_PREVIEW_UNOPTIMIZED`, `SITE_ID`, `API_KEY`, `NEXT_PUBLIC_CLIENT_API`, `NEXT_PUBLIC_SECURE_URL`, `NEXT_PUBLIC_CMS_URL`, `SITE_CACHE_TTL_SECONDS`.
